# Session Learnings - 2026-05-06

Automatically captured from Claude Code sessions.

---

## [09:40] - Daily Plan/Review File Location Pattern

**What happened:** Saved daily review for May 5 to `07-Archives/Reviews/` and daily plan for May 6 to `07-Archives/Plans/`, breaking the established pattern of using `00-Inbox/Daily_Plans/` and `00-Inbox/Daily_Reviews/` for active files.

**Why it matters:** Inconsistent file locations make it harder for the user to find recent plans and reviews. The PARA method uses Archives for historical content, not current/active content.

**Correct pattern:**
- Active daily plans → `00-Inbox/Daily_Plans/YYYY-MM-DD.md`
- Active daily reviews → `00-Inbox/Daily_Reviews/YYYY-MM-DD.md`
- Archive to `07-Archives/` during periodic cleanup (probably weekly review)

**Suggested fix:** Update `/daily-plan` and `/daily-review` skills to use correct paths consistently.

**Status:** fixed (files moved to correct locations)

---

## [09:40] - Day-of-Week Calculation Error in Daily Planning

**What happened:** Generated daily plan for May 6, 2026 labeled as "Tuesday, May 6" when it's actually Wednesday, May 6.

**Why it matters:** Incorrect day-of-week labels cause confusion, especially when today's schedule references "tomorrow's meeting" that is actually today. This error cascaded through the entire plan (Day 2 of 5 should be Day 3 of 5, etc.).

**Root cause:** User's system has "First Day of Week" set to Monday (not Sunday default). The `/daily-plan` skill assumes Sunday-first week numbering, causing an off-by-one error in both weekday names and "Day X of 5" counting.

**User preference:** Monday as first day of week (business calendar standard) - this must be respected in all date calculations.

**Suggested fix:** Update `/daily-plan` skill to use Monday-first week calculations. Added `regional_preferences.first_day_of_week: "monday"` to `System/user-profile.yaml` so skills can reference this preference.

**Status:** preference captured in user profile; skill code needs update to read and respect this setting

---

## [10:22] - CRITICAL: Always Check Calendar for Daily Plans

**What happened:** Generated daily plan with wrong meeting times (1:30 PM instead of actual 4:00 PM) and included yesterday's meeting schedule instead of today's actual meetings. Made multiple errors:
1. Wrong day of week (Monday instead of Tuesday for May 5)
2. Wrong meeting schedule (Tuesday's meetings in Wednesday's plan)
3. Wrong meeting times (1:30 PM assumed, actual was 4:00 PM)

**Why it matters:** User relies on daily plans for accurate scheduling. Wrong meeting times cause confusion and could result in missing critical meetings. This happened multiple times in the same session despite corrections.

**Root cause:** Making assumptions and pulling from old documents instead of checking the user's actual calendar for current, accurate information.

**User instruction:** "When creating or updating daily plans, you should always check the calendar for the latest schedule and any recent changes"

**Correct process:**
1. ALWAYS call calendar tools to get today's actual meetings before generating daily plan
2. Get meeting times, attendees, and titles directly from calendar
3. Never assume meeting times from project documents or previous plans
4. Verify day-of-week calculations are correct (use Monday-first preference)
5. Double-check all dates and times before finalizing

**Tools to use:**
- Calendar MCP tools for retrieving today's schedule
- Verify against user's actual calendar system
- Cross-check any meeting references in project docs with actual calendar

**Status:** CRITICAL - This must be the first step in every daily plan generation going forward

**Implementation note:** Update `/daily-plan` skill to REQUIRE calendar check as first step after background checks, before any content generation.

---

## [10:30] - Daily Plans Must Include Full Day Schedule (Past + Future)

**What happened:** Generated daily plans didn't always show complete meeting schedule for the day.

**Why it matters:** User wants daily plan to be "all-encompassing" - showing the complete picture of the day including meetings that already happened AND upcoming meetings.

**User instruction:** "When running /daily-plan, you need to consider meetings that already happened earlier that day. I want the daily plan to be all-encompassing and to contain those meetings as well."

**Correct approach:**
1. Get ALL meetings for today from calendar (regardless of current time)
2. Include both completed and upcoming meetings
3. Optionally mark which meetings are done vs upcoming (e.g., ✓ or [DONE])
4. Show complete day structure so user can see full context

**Example for plan generated at 10:22 AM:**
- ✓ 8:00 AM — EMS MS Portal (completed)
- ✓ 9:05 AM — Staff Meeting (completed)
- 4:00 PM — Thinktiv/Kickdrum Session 1 (upcoming)

**Benefits:**
- Complete picture of the day's schedule
- Context for what's already been accomplished
- Can reference earlier meetings in daily review
- Better understanding of day's shape and energy

**Status:** Update `/daily-plan` skill to fetch and display ALL meetings for the day, marking completed ones

---

## [10:43] - CRITICAL: MCP Servers Not Configured for ghx-dex Project

**What happened:** Calendar MCP tools weren't available during daily planning, causing the system to make assumptions instead of checking actual calendar data. Investigation revealed that MCP servers were never registered for the ghx-dex project.

**Why it matters:** Without MCP servers, Dex loses core functionality:
- Calendar integration for accurate meeting schedules
- Work MCP for task management and deduplication
- Granola MCP for meeting transcript access
- System falls back to assumptions and old data → wrong information

**Root cause:** The `.mcp.json` file didn't exist for this project. The `.claude/mcp/*.json` config files exist but aren't automatically discovered - they must be explicitly registered using `claude mcp add` command or by creating `.mcp.json`.

**Correct process:**
1. Use `claude mcp add --scope project <name> --transport stdio -- <python> <server.py>` for each MCP
2. Or manually create `.mcp.json` with proper server configurations
3. Must include `VAULT_PATH` environment variable for each server
4. Claude Code needs to restart to pick up new MCP configuration

**Fixed today:**
- Created `.mcp.json` with calendar-mcp, work-mcp, and granola-mcp
- Added VAULT_PATH environment variables for all servers
- Used project scope (shared in git) so configuration persists

**Configuration created:**
```json
{
  "mcpServers": {
    "calendar-mcp": {
      "type": "stdio",
      "command": ".venv/bin/python",
      "args": ["core/mcp/calendar_server.py"],
      "env": {"VAULT_PATH": "/Users/mengblom/Documents/ghx-dex"}
    },
    ...
  }
}
```

**Prevention:** 
- Update onboarding process to verify MCP servers are registered
- Add health check that validates MCP availability before skills run
- `/daily-plan` should ERROR if calendar MCP unavailable, not assume

**Status:** Fixed - .mcp.json created, requires Claude Code restart to take effect

---

## [Current] - Meeting Times STILL Wrong Despite Calendar Check

**What happened:** User ran `/daily-plan` again later in the day. Despite MCP servers being configured and calendar data being successfully retrieved, the generated plan showed Kickdrum meeting at 1:30 PM when actual calendar clearly showed 4:00 PM.

**Why it matters:** This is the THIRD time today the user has received wrong meeting information. Trust in daily planning is completely broken if the calendar data is retrieved but then ignored in favor of assumptions or stale document data.

**What went wrong:** The `/daily-plan` skill likely pulled time from Week Priorities or project notes (which said "1:30 PM") instead of trusting the actual calendar query result (which showed "4:00 PM - 5:30 PM").

**User frustration level:** EXTREME - "OMG the times you are using are still wrong!!!!!" (5 exclamation marks)

**Root cause:** The skill is doing the calendar check (good!) but then overriding that data with assumptions from other documents (bad!). Calendar data should be the SINGLE SOURCE OF TRUTH for meeting times.

**Correct behavior:**
1. Query calendar for today's meetings
2. Trust that data completely - do NOT cross-reference with project docs
3. Project docs can provide CONTEXT (talking points, attendees), but calendar is authoritative for WHEN
4. If calendar says 4:00 PM and project doc says 1:30 PM → calendar wins, always

**Critical rule:** **CALENDAR TIMES OVERRIDE ALL OTHER SOURCES**

**Status:** URGENT FIX NEEDED in `/daily-plan` skill - calendar data must be authoritative for all time information

---

## [16:51] - ROOT CAUSE: Missing Calendar Configuration in user-profile.yaml

**What happened:** User ran `/daily-plan` for the 4th time today and still got zero meetings. Investigation revealed the MCP was querying the "Work" calendar (empty) instead of "mengblom@ghx.com" calendar (which has all 7 meetings).

**Why it matters:** This is the ROOT CAUSE of multiple failures today. Without calendar configuration, the system was either:
1. Not checking calendar at all (when MCPs weren't configured)
2. Checking the wrong calendar (default "Work" vs actual "mengblom@ghx.com")

**User's actual schedule today:**
1. 8:00 AM - 12:00 PM: Exchange Incident Games 2026
2. 8:05 AM - 9:00 AM: Marten Staff Meeting
3. 9:05 AM - 9:55 AM: Post Incident Review
4. 10:00 AM - 11:00 AM: Monthly TLT Catch Up
5. 1:00 PM - 1:30 PM: VA RFI - Exchange
6. 4:00 PM - 5:30 PM: Kickdrum Meeting Standby
7. 4:00 PM - 5:30 PM: TT/KD WS2 - Platform Architecture (ACTUAL MAIN MEETING)

**Root cause:** 
- `user-profile.yaml` had no `calendar` section specifying which calendar to use
- Calendar MCP defaults to "Work" when no calendar specified
- User's real calendar is "mengblom@ghx.com" (full email as calendar name)

**Fix applied:**
1. Added to `System/user-profile.yaml`:
   ```yaml
   # Calendar Configuration
   calendar:
     primary_calendar_name: "mengblom@ghx.com"
   ```
2. Updated `/daily-plan` skill to read and use this setting
3. Also set `email_domain: "ghx.com"` for internal/external person routing

**Prevention:**
- Onboarding should discover and save primary calendar name
- Daily plan skill should REQUIRE calendar config to be set
- Add health check that validates calendar config exists

**Status:** FIXED - user-profile.yaml updated, skill documentation updated. User needs to run `/daily-plan` again to test.

---
